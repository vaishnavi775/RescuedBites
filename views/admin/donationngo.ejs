<main>
	<%- include('../partials/adminSidebar') %>
	
	<div id="main-wrapper">
		
		<div class="bg-white shadow-sm p-3">
			<span class="me-3" id="sidebar-toggler-btn"><i class="fas fa-bars"></i></span>
			<h5 class="m-0 color-theme d-inline-block">Previously collected Donation</h5>
		</div>
		<div  class="prof mb-4 p-4 shadow-sm" style="margin-left:2rem ;margin-right:2rem">
		<% for(let i=0; i<donation.length; i++) { %>
			<div class="mb-2">
				<span>Food Name:</span>
				<span><%= donation[i].foodName %></span>
			</div>
			<div class="mb-2">
				<span>Food Tag:</span>
				<span><%= donation[i].foodTag %></span>
			</div>
			
			<div class="mb-2">
				<span>Quantity:</span>
				<span><%= donation[i].quantity %></span>
			</div>
			
			
			<% if(donation[i].donorToNgoMsg != "") { %>
				<div class="mb-2">
					<span>Message from Donor:</span>
					<div class="ms-3"><%= donation[i].donorToNgoMsg %></div>
				</div>
			<% } %>
				
			<div class="mb-2">
				<span>Status:</span>
				<span class="fw-bold text-<%= donation.status %>"><%= donation[i].status %></span>
			</div>
			
			<% if(donation[i].status == "collected") { %>
				<div class="mb-2">
					<span>Collection time:</span>
					<span><%= donation[i].collectionTime.toLocaleString("en-IN", { dateStyle: "medium", timeStyle: "short"}) %></span>
				</div>
			<% } %>
			<hr colour="blue">
		<% } %>	
		
		</div>
		<div style="margin-left:3%"><a href="/admin/donor/<%= ngoId %>" class="btn">Back</a></div>	
	</div>
</main>